# Simple Github Action (CI/CD) React.js tutorial
This is simple CI/CI pipeline example of React.js. <br />
Once we push the code, the action are going to happen automatically, it will first test the code.